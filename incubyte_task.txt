DROP TABLE IF EXISTS #test
CREATE TABLE #test(H VARCHAR(20),C_NAME VARCHAR(255), Cust_ID VARCHAR(18), OPEN_DT DATE, CONSULT_DT DATE, VAC_ID 
VARCHAR(20),DR_CONSULT VARCHAR(255),STATE VARCHAR(20), COUNTRY VARCHAR(20),  DOB VARCHAR(20)
, FLAG VARCHAR(20))
BULK INSERT #test 
   FROM 'D:\kinjal_task\readfile.txt'
   WITH 
      (
        FIELDTERMINATOR = '|',
		ROWTERMINATOR = '|D',
		FIRSTROW=2
      )


SELECT C_NAME,Cust_ID,CONVERT(DATE,OPEN_DT,103) AS OPEN_DT, CONVERT(DATE,CONSULT_DT,103) AS CONSULT_ID, VAC_ID,DR_CONSULT, STATE, COUNTRY,
cast(right(DOB, 4)+substring(DOB, 3, 2)+left(DOB, 2) as date) AS DOB
,FLAG FROM #test
